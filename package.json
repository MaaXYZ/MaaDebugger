{
  "name": "maa-debugger-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:skip-frontend": "node build.mjs --skip-frontend",
    "build:linux": "node build.mjs --os linux",
    "build:windows": "node build.mjs --os windows",
    "build:darwin": "node build.mjs --os darwin"
  }
}
